{
  "name": "spanish-daily-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only static website: a projection of the canonical lessons in Git.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/assert-build.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.1.3",
    "marked": "^18.0.7"
  }
}
